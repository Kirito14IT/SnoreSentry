--- a/applications/xiaozhi/wake_word/meow_detect_once.cpp
+++ b/applications/xiaozhi/wake_word/meow_detect_once.cpp
@@
+#include "snore_backend_client.h"
@@
 /* when entering snore detection mode */
+snore_backend_set_detecting(RT_TRUE);
@@
 /* inside the inference loop, before / after the model call */
+const int64_t window_end_local_ms = (int64_t)rt_tick_get_millisecond();
+const int64_t window_start_local_ms = window_end_local_ms - 2000;
+const int64_t inference_done_local_ms = (int64_t)rt_tick_get_millisecond();
+snore_backend_on_score(
+    score,
+    detected ? RT_TRUE : RT_FALSE,
+    window_start_local_ms,
+    window_end_local_ms,
+    inference_done_local_ms
+);
@@
 /* any early exit, stop path, or stream-open failure */
+snore_backend_set_detecting(RT_FALSE);

Recovery intent:
- keep the backend-facing detection state consistent with the real detector lifecycle
- upload one score point per inference hop
- upload one event per detected snore hit
- preserve local detector behavior while adding backend synchronization
