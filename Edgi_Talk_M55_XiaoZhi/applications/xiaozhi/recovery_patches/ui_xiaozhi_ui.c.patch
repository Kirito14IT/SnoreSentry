--- a/applications/xiaozhi/ui/xiaozhi_ui.c
+++ b/applications/xiaozhi/ui/xiaozhi_ui.c
@@
+void xiaozhi_ui_exit_meow_mode_remote(void)
+{
+    /*
+     * Use the same internal exit path as the local meow-mode back button,
+     * but callable from snore_backend_client.cpp when the backend sends
+     * a remote stop command.
+     */
+}
@@
 static void meow_back_btn_event_cb(lv_event_t *e)
 {
-    /* original inline meow-mode exit logic */
+    xiaozhi_ui_exit_meow_mode_remote();
 }

Recovery intent:
- de-duplicate the meow-mode exit implementation
- make remote stop and local back-button stop share one code path
